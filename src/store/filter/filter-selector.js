export const allFilterSelector = (state) => state.filter;
export const inputSelector = (state) => state.filter.input;
export const statusSelector = (state) => state.filter.status;
export const dropdownStateSelector = (state) => state.filter.dropdownState;
export const episodesSelector = (state) => state.filter.episodes;